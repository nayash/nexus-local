# UI Framework
flet[all]==0.80.4

# Core Utilities
requests
python-dotenv
pydantic
numpy
pandas

# Vector Database & PDF
lancedb
pypdf

# Web Search — uses GoogleSerperAPIWrapper from langchain-community (already listed below)
# Obtain a free API key at https://serper.dev and set SERPER_API_KEY in your .env

# AI & Orchestration (Pinned for stability)
langchain-core>=0.3.15
langchain>=0.3.15
langchain-community>=0.3.15
langchain-classic>=0.3.15
langchain-ollama>=0.2.0
langchain-text-splitters>=0.3.0
langgraph

# File Watching
watchdog

# Parser
lark

# Docker Sandbox (optional — only needed if CODE_SANDBOX_ENGINE=docker)
docker>=7.0.0